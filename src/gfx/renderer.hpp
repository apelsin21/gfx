#ifndef RENDERER_HPP
#define RENDERER_HPP

namespace gfx {
    class Renderer {
        protected:
        public:
            Renderer();
            virtual ~Renderer();
    };
}

#endif //RENDERER_HPP
